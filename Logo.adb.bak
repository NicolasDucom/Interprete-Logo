WITH Ada.Text_IO;USE Ada.Text_IO;
with p_fenetre; use p_fenetre;
with ada.Integer_Text_IO; use ada.Integer_Text_IO;
with p_logo; use p_logo;
PROCEDURE Logo IS
c : String(1..10);
int : integer;
   N : Natural;
   op:command;
BEGIN
  -- p_fenetre.Create_Graph_Window;
  -- if p_fenetre.Is_Open then
  --    put("is open");
  -- else
  --    put("is not open");
 --  END IF;
 --  Draw_Line(12,56,34,87);
   Op.Action:=(OTHERS=>'#');
   While NOT p_logo.CompareStrings(Op.Action,textToAction("exit")) LOOP
      Op:=P_Logo.GetAction;
   Put(Op.Action);
   Put(Op.Value);
   IF p_logo.CompareStrings(Op.Action,textToAction("coucou")) THEN
      Ada.Text_IO.Put_line("youpi");
   END IF;
   END LOOP;

End Logo;
