WITH Ada.Text_IO;USE Ada.Text_IO;
with p_fenetre; use p_fenetre;
with ada.Integer_Text_IO; use ada.Integer_Text_IO;
with p_logo; use p_logo;
PROCEDURE Logo IS
c : String(1..10);
int : integer;
   N : Natural;
   op:operation;
BEGIN
  -- p_fenetre.Create_Graph_Window;
  -- if p_fenetre.Is_Open then
  --    put("is open");
  -- else
  --    put("is not open");
 --  END IF;
 --  Draw_Line(12,56,34,87);
   Op.Action:=(OTHERS=>"#");
   While p_logo.CompareStrings(Op.Action,TextToString("exit")) LOOP

   END LOOP;

End Logo;
